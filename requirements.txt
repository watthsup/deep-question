# Minimal POC stack. Install into conda env "deep-question":
#   conda activate deep-question && pip install -r requirements.txt
langchain-core>=1.0
langchain-anthropic>=1.7      # ChatAnthropic — Anthropic direct
langchain-aws>=1.7            # ChatBedrockConverse — standard AWS Bedrock connector
boto3>=1.34.0                 # AWS SDK for boto3 session and Bedrock runtime
pandas>=2.0
openpyxl>=3.1                 # Excel read/write
python-dotenv>=1.0
pydantic>=2.0
